using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Attacker : MonoBehaviour
{
    void Attack(SearchMap points)
    {
        Vector2Int attackrPos = Vector2Int.zero;
        Vector2Int[] targets = null;
        int point = 0;
        if (true)
        {
            point++;
        }
        if (true)
        {
            point++;
        }
        //points[attackrPos].TotalCost = point;
    }
}
